{:paths ["src"]
 ;; Advertisement IPLD is a logical value here. dag-cbor encode, CID hash,
 ;; HTTP, JSON, signatures and clocks are injected. This library holds no
 ;; crypto, no socket and no clock — same seam as kad.routing.
 :deps {}
 :aliases
 {:test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}
                      ;; TEST-ONLY. The Kotoba compiler and the KIR
                      ;; interpreter drive the `head.kotoba` port through
                      ;; `head-parity-test`, which checks it against the
                      ;; reference in `ipni.head`. Neither is linked into
                      ;; anything this repo ships.
                      io.github.kotoba-lang/kotoba-kir
                      {:git/url "https://github.com/kotoba-lang/kotoba-kir.git"
                       :git/sha "136a62e0ad93585c5ecfa6755c82aa4ac2637e14"}
                      io.github.kotoba-lang/amu
                      {:git/url "https://github.com/kotoba-lang/amu.git"
                       :git/sha "6889fa736750348863d2b98d69e688131a2ca515"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts ["-m" "clj-kondo.main" "--lint" "src" "test"
                     "--fail-level" "error"]}}}
